# Public mobile-app configuration. EXPO_PUBLIC_* values are visible in the app bundle.
# Use a reachable LAN address rather than localhost when testing with a physical device.
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000/api
